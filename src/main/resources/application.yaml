spring:
  datasource:
    url: jdbc:mysql://localhost:3306/guessing_game
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: mysecretkey
  expiration: 86400000